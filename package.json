{
  "name": "aubasa-proveedores",
  "version": "1.0.0",
  "description": "Sistema de evaluación de proveedores críticos AUBASA",
  "scripts": {
    "build": "npm install --prefix frontend && npm run build --prefix frontend && npm install --prefix backend",
    "start": "node backend/server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
